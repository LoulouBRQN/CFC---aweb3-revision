info:
  name: GET Classe inexistante
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/cours?classe=CLASSE-INEXISTANTE"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("HTTP 404", function() {
          expect(res.getStatus()).to.equal(404);
        });

        test("Message d'erreur présent", function() {
          expect(res.getBody()).to.have.property("error");
        });
